This folder contains sample artifacts generated during pipeline execution, including:

- Drift detection outputs
- Model evaluation metrics
- Preprocessing artifacts (imputer, scaler)

These mirror outputs stored in the S3 staging layer.